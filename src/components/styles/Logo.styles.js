import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  logoContainer: {
    flex: 1,
    paddingTop: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  logo: {
    width: 100,
    height: 100,
  },
})
